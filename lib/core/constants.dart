import 'package:flutter/material.dart';
class MyConstans extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.symmetric(vertical: 8.0),
      child: Container(
        height: 200,
        color: Colors.white,
        child: Text('',
          style: TextStyle(
          color: Colors.black12,
    ),
      ),),
    );
  }
}